{
  "name": "xdrinternals-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Microsoft Defender XDR incident investigation, response handling and threat hunting, backed by the XDRInternals PowerShell module.",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "bin": {
    "xdr-mcp-server": "dist/index.js"
  },
  "files": [
    "dist",
    "bridge",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js",
    "smoke": "node dist/index.js --smoke-test",
    "inspect": "npx -y @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.7.0"
  }
}
